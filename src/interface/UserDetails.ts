export interface UserDetails {
    id_user: number;
    user: string;
    first_name: string;
    last_name: string;
    email: string;
    password: string;
    role: number;
    created_at: string;
    // Agrega las demás propiedades del usuario según su estructura
  }